<form method="post" action="{{ path('app_produit_delete', {'produitreference': produit.produitreference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.produitreference) }}">
    <button class="btn">Delete</button>
</form>
